apiVersion: v1
kind: Service
metadata:
  name: hjtdrdyuuiu
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hjtdrdyuuiu"
    port: 8080
  selector:
    app: hjtdrdyuuiu
